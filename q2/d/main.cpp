#include <iostream.h>
#include <math.h>

using namespace std;

int main(){

    // I
    char MyVariable = "A";

    // II
    int a, b, c;
    float p;
    p = (7.0*c * ( 6.0*a - 3.0*( 8.0*b - 4.0*c ) - 90.0) + 2.0*a + 4.0*b) / 10.0;

    // III
    float s, v, u, a;
    s = (pow(v,2.0) - pow(u,2.0))/(2.0*a);

    /// IV
    double a, b, c, d;
    d = -(-b + pow((pow(b, 2.0) - 4.0 * a * c), 1.0/2.0))/(2.0 * a);

    // V
    double a, r, t, g;
    a = sqrt( (pow( (t) ,2) ) - 2.0 * (pow( r,2.0 ) * (b - 3.0*g) - 7.0 ) + 9.0 );

    // VI
    double a, r;
    r = pow((a*(1.0/3.14)), 1.0/2.0);

    return 0;
}
